{
  "name": "transcribe",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/server"
  ],
  "devDependencies": {
    "turbo": "^1.0.0",
    "ts-node-dev": "^2.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "start": "turbo run start",
    "dev": "turbo run dev"
  }
}
