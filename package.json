{
  "name": "transcribe",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@7.0.0",
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/server"
  ],
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "turbo": "2.3.4"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "start": "turbo run start",
    "dev": "turbo run dev"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
