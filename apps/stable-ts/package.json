{
  "name": "stable-ts",
  "version": "1.0.0",
  "scripts": {
    "venv": "bash -c 'source venv/bin/activate'",
    "build": "echo 'No build step for Python app'",
    "lint": "",
    "test": "pytest",
    "start": "bash -c 'source venv/bin/activate && venv/bin/python app.py'",
    "installPython": "pip install -r requirements.txt",
    "dev": "bash -c 'source venv/bin/activate && venv/bin/python app.py'"
  }
}