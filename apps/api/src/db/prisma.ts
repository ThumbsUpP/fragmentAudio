/**
 * Prisma schema location for the v2 database model.
 *
 * The schema is committed in this package so migrations and repositories can be
 * kept aligned with the future generated Prisma Client. Runtime access in this
 * PR is handled by the lightweight PostgreSQL pool in `pool.ts` until the
 * package registry allows installing Prisma/Zod artifacts.
 */
export const prismaSchemaPath = "apps/api/prisma/schema.prisma";
