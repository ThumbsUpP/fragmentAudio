/**
 * Prisma client placeholder.
 *
 * The v2 schema is already staged in `apps/api/prisma/schema.prisma`. The actual
 * `@prisma/client` dependency and generated client will be enabled in the next
 * database implementation PR, once dependency installation is available.
 */
export const prismaSchemaPath = "apps/api/prisma/schema.prisma";
