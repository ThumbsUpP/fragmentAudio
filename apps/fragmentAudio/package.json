{
  "name": "python-app",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step for Python app'",
    "lint": "flake8 .",
    "test": "pytest",
    "start": "python app.py",
    "init": "pip install -r requirements.txt",
    "dev": "python app.py"
  }
}
